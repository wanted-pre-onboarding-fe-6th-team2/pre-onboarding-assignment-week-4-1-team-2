import { GridItem } from '@chakra-ui/react';
import styled from '@emotion/styled';

const FooterContainer = styled(GridItem)`
  display: flex;
  justify-content: center;
  align-items: center;
`;

export { FooterContainer };
